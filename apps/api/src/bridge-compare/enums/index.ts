export enum RankingMode {
  BALANCED = 'balanced',
  LOWEST_COST = 'lowest-cost',
  FASTEST = 'fastest',
}

export enum BridgeStatus {
  ACTIVE = 'active',
  DEGRADED = 'degraded',
  OFFLINE = 'offline',
}

export enum SupportedChain {
  STELLAR = 'stellar',
  ETHEREUM = 'ethereum',
  POLYGON = 'polygon',
  BINANCE = 'binance',
  AVALANCHE = 'avalanche',
  ARBITRUM = 'arbitrum',
  OPTIMISM = 'optimism',
}
